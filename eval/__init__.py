"""eval — AI Governance Lab Benchmark Suite (L1+L2+L3).

L1: 6 injected scenarios + 1 natural emergence, with ground-truth labels + scoring harness.
L2: Parameterized grid (templates × models × seeds) + aggregated per-model CI95.
L3: Open audit (already built via afi.audit — compute_awi + run_monitor + html_report).
"""
